# 潮汐渔场 · 深海增量版

当前版本是纯文字、数值和按钮驱动的深海增量游戏，不加载 3D、GLB、图片或海洋动画。

## 直接运行

双击 `index.html` 可继续使用本地离线模式。

## GitHub Pages 发布

1. 把当前目录中的全部文件上传到一个公开 GitHub 仓库的根目录。
2. 在仓库 `Settings → Pages` 选择 `main` 分支的 `/ (root)` 目录。
3. 访问 `https://用户名.github.io/仓库名/`。
